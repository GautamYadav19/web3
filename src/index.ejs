<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Todo application</h1>
    <%if(todos.length>0){%>
    <h2>List of work is empty right now .</h2>
    <% }else{%> <%for(let todo of todos){ %>
    <p><%=todo.text %></p>
    <%}%> <%}%>
  </body>
</html>
